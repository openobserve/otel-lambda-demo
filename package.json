{
    "name": "lambda-opentelemetry-openobserve-demo",
    "version": "1.0.0",
    "description": "Demo Lambda functions with OpenTelemetry and OpenObserve integration",
    "main": "index.js",
    "scripts": {
      "test": "echo \"Error: no test specified\" && exit 1",
      "build": "npm install",
      "deploy": "sam build && sam deploy --guided",
      "local": "sam local start-api"
    },
    "keywords": [
      "aws",
      "lambda",
      "opentelemetry",
      "openobserve",
      "observability",
      "tracing",
      "logging"
    ],
    "author": "Your Name",
    "license": "MIT",
    "dependencies": {
      "@opentelemetry/api": "^1.7.0",
      "aws-sdk": "^2.1490.0"
    },
    "devDependencies": {
      "@types/aws-lambda": "^8.10.126"
    },
    "engines": {
      "node": ">=18.0.0"
    }
  }